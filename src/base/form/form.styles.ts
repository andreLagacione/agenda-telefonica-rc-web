import styled from 'styled-components';

const Form = styled.div`
    .validate-message {
        font-size: 13px;
    }

    .icon {
        position: absolute;
        right: 25px;
        top: 34px;
    }
`;

export default Form;